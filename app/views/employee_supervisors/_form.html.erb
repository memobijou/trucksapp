<%= form_for(@employee_supervisor) do |f| %>
  <% if @employee_supervisor.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@employee_supervisor.errors.count, "error") %> prohibited this employee_supervisor from being saved:</h2>

      <ul>
      <% @employee_supervisor.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :employee_id %><br>
    <%= f.text_field :employee_id %>
  </div>
  <div class="field">
    <%= f.label :supervisor_id %><br>
    <%= f.text_field :supervisor_id %>
  </div>
  <div class="field">
    <%= f.label :priority %><br>
    <%= f.number_field :priority %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
